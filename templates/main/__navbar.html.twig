
{% set sessionVersion = app.session.get("version") %}
<nav class="navbar navbar-expand-lg navbar-light bg-primary mb-2 sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">APILOL</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link " href="{{ path('summoner_index') }}">Summoner</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('champions_index') }}">Champions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('rank_index') }}">Classement</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('item_index') }}">Items</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Statistiques
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('statAll') }}">Champions</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_changeSession') }}">{{ app.session.get('version') }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('world_index') }}">World 2021</a>
                </li>
                {% if app.user is not null %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('suggestion_list') }}">Suggestion</a>
                    </li>
                {% endif %}
            </ul>
            <ul class="nav navbar-nav">
                {% if app.user is not null %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
